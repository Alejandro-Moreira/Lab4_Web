// Método 1
function saludar(nombre) {
    console.log("¡Hola, " + nombre + "espero tengas un buen dia");
  }
  
  // Llamada al método con un nombre específico
  saludar("Alejandro");

  

  // Método 2 : Suma dos números
function sumar(a, b) {
    return a + b;
  }
  
  // Llamada al método con dos números
  let resultado = sumar(5, 3);
  console.log("La suma es: " + resultado);